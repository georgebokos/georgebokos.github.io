# In-App Review — τι πρέπει να γίνει στο native project

Ο **web κώδικας είναι ήδη έτοιμος** και ανεβασμένος. Το `index.html` καλεί το
plugin αν υπάρχει, και πέφτει στο Play Store αν λείπει — δηλαδή η TWA και ο
browser συνεχίζουν να δουλεύουν όπως πριν, χωρίς καμία αλλαγή.

Μένουν μόνο τα βήματα στο **τοπικό project `fooddaily-app`** (Capacitor).

---

## 1. Εγκατάσταση του plugin

```bash
cd fooddaily-app
npm install @capacitor-community/in-app-review
npx cap sync android
```

Δεν χρειάζεται καμία αλλαγή σε Java/Kotlin: το Capacitor καταχωρεί μόνο του το
plugin κατά το `sync`.

## 2. versionCode +1

Στο `android/app/build.gradle`:

```gradle
defaultConfig {
    applicationId "com.fooddaily.app"
    versionCode 21        // ← +1 από την τρέχουσα τιμή
    versionName "2.0.3"
}
```

**Αυτό δεν είναι προαιρετικό.** Είναι και κανόνας του Play (ο `versionCode`
πρέπει πάντα να αυξάνεται) και η πιθανή λύση στο πρόβλημα των αξιολογήσεων:
όσο η κλειστή δοκιμή έχει τον **ίδιο** `versionCode` με την παραγωγή, οι
συσκευές παραμένουν χρεωμένες στο κανάλι δοκιμής και το Play δείχνει «ιδιωτικό
σχόλιο» αντί για αστέρια.

## 3. Build και ανέβασμα στην παραγωγή

```bash
npx cap open android      # ή: ./gradlew bundleRelease
```

Ανέβασε το `.aab` **στην παραγωγή**, όχι σε κανάλι δοκιμής.

## 4. Διακοπή όλων των καναλιών δοκιμής

Play Console → **Δοκιμή** → για καθένα από **εσωτερική, κλειστή, ανοιχτή**:
αν υπάρχει ενεργή κυκλοφορία, **«Διακοπή δοκιμής»**.

---

## Πώς επαληθεύεται ότι δούλεψε

Το In-App Review **δεν εμφανίζεται πάντα** — η Google το περιορίζει με ποσόστωση
ανά χρήστη, και δεν δίνει κανένα σφάλμα όταν το κρύβει. Οπότε:

- **Μην** κρίνεις από το αν εμφανίστηκε στο δικό σου κινητό.
- Κρίνε από τα **Στατιστικά → Βαθμολογίες** στο Play Console, μετά από μερικές
  μέρες με πραγματικούς χρήστες.

Για δοκιμή του κώδικα (όχι της εμφάνισης), χρησιμοποίησε **internal app sharing**
του Play Console — εκεί το API απαντά κανονικά.

---

## Τι άλλαξε ήδη στον web κώδικα

| | Πριν | Τώρα |
|---|---|---|
| Κατώφλι 1ης προτροπής | 5 συνταγές | **3** |
| Μετά από «Αργότερα» | 10 → 20 → 40 → 80 | **6 → 12 → 24 → 24** |
| Τρόπος αξιολόγησης | μόνο άνοιγμα Play Store | **In-App Review**, με fallback στο Play Store |

Ο διπλασιασμός σταματά πλέον στις 24 συνταγές. Χωρίς οροφή, δύο «Αργότερα»
έστελναν το επόμενο κατώφλι στις 20 και τρία στις 40 — δηλαδή πρακτικά ποτέ
ξανά για τον καθημερινό χρήστη.

## Γιατί το plugin ελέγχεται με `reviewPlugin()`

Ίδιο μοτίβο με `ttsPlugin()`, `sharePlugin()`, `hasNativeBilling()`: η TWA δεν
έχει Capacitor, οπότε κάθε native λειτουργία πρέπει να υποβαθμίζεται καθαρά
εκεί. Ο κανόνας είναι γραμμένος στο `CLAUDE.md` — **μην τον παρακάμψεις**.
