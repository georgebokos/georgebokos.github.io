# Νέο build στην παραγωγή — αναλυτικά βήματα

Στόχος: η **παραγωγή** να έχει τον **μεγαλύτερο `versionCode`** και την πιο
πρόσφατη ημερομηνία από κάθε κανάλι δοκιμής, ώστε οι συσκευές να ξεκολλήσουν
από το κανάλι δοκιμής και να εμφανιστούν επιτέλους τα αστέρια.

---

## Πριν ξεκινήσεις: τι ΔΕΝ χρειάζεται build

Οι αλλαγές στο `index.html` και στο `service-worker.js` (κατώφλι προτροπής,
γερμανικά, διορθώσεις γιορτών) **είναι ήδη live** — η εφαρμογή φορτώνει το
site, δεν το πακετάρει. Το build χρειάζεται **μόνο** για δύο πράγματα:

1. το plugin του In-App Review (native κώδικας)
2. τον νέο `versionCode`

---

## ΒΗΜΑ 1 — Κατέγραψε τους τρέχοντες αριθμούς

Play Console → **Δοκιμή και κυκλοφορία** → **Επισκόπηση εφαρμογής**.
Σημείωσε τον `versionCode` κάθε καναλιού:

| Κανάλι | versionCode | Ενεργό; |
|---|---|---|
| Παραγωγή | ___ | |
| Ανοιχτή δοκιμή | ___ | |
| Κλειστή δοκιμή | ___ | |
| Εσωτερική δοκιμή | ___ | |

**Ο νέος αριθμός πρέπει να είναι μεγαλύτερος από ΟΛΟΥΣ**, όχι μόνο από της
παραγωγής. Αν π.χ. η κλειστή δοκιμή έχει 20 και η παραγωγή 20, βάλε **21**.

Εναλλακτικά, το βρίσκεις και τοπικά:

```bash
cd fooddaily-app
grep versionCode android/app/build.gradle
```

## ΒΗΜΑ 2 — Σταμάτησε ΟΛΑ τα κανάλια δοκιμής

Για καθένα από **εσωτερική, κλειστή, ανοιχτή** που έχει ενεργή κυκλοφορία:

Play Console → **Δοκιμή** → *(κανάλι)* → καρτέλα **Εκδόσεις** → στην ενεργή
έκδοση, μενού **⋮** → **«Διακοπή κυκλοφορίας»** (Halt rollout).

Κάν' το **πριν** το ανέβασμα. Δεν χάνεις τίποτα: το build είναι ήδη στην
παραγωγή, οπότε όσοι το είχαν συνεχίζουν κανονικά.

## ΒΗΜΑ 3 — Εγκατάστησε το plugin

```bash
cd fooddaily-app
npm install @capacitor-community/in-app-review
npx cap sync android
```

Το `sync` καταχωρεί μόνο του το plugin — δεν αγγίζεις Java/Kotlin.

## ΒΗΜΑ 4 — Ανέβασε τον versionCode

Άνοιξε `android/app/build.gradle`:

```gradle
defaultConfig {
    applicationId "com.fooddaily.app"
    versionCode 21          // ← ο αριθμός του ΒΗΜΑΤΟΣ 1, +1
    versionName "2.0.3"     // ← ό,τι θέλεις, το βλέπει ο χρήστης
    ...
}
```

- `versionCode` — ακέραιος, **πάντα αυξάνεται**, δεν τον βλέπει ο χρήστης.
- `versionName` — το κείμενο στη σελίδα του Play (τώρα 2.0.2).

## ΒΗΜΑ 5 — Χτίσε το .aab

```bash
cd android
./gradlew clean
./gradlew bundleRelease
```

Το αρχείο βγαίνει στο:
`android/app/build/outputs/bundle/release/app-release.aab`

**Υπογραφή:** πρέπει να χρησιμοποιηθεί το **ίδιο `signing.keystore`** με τις
προηγούμενες εκδόσεις. Αν το Play απορρίψει το ανέβασμα με μήνυμα για
«λάθος πιστοποιητικό», το keystore είναι λάθος — μην το αντικαταστήσεις,
βρες το σωστό. Χωρίς αυτό δεν μπορεί να γίνει καμία ενημέρωση της εφαρμογής.

## ΒΗΜΑ 6 — Ανέβασέ το στην ΠΑΡΑΓΩΓΗ

Play Console → **Δοκιμή και κυκλοφορία** → **Παραγωγή** →
**«Δημιουργία νέας έκδοσης»**

1. Ανέβασε το `app-release.aab`
2. Στις **σημειώσεις έκδοσης** γράψε τι άλλαξε, π.χ.:

```
• Γερμανικά — όλες οι συνταγές και η εφαρμογή
• Αξιολόγηση μέσα από την εφαρμογή
• Διορθώσεις στις εορταστικές προτάσεις
```

3. **Έλεγχος → Έναρξη κυκλοφορίας στην παραγωγή**

## ΒΗΜΑ 7 — Κυκλοφορία στο 100%

Αν το Play προτείνει **σταδιακή κυκλοφορία** (π.χ. 20%), ανέβασέ τη στο
**100%**. Με σταδιακή, ένα μέρος των χρηστών δεν παίρνει ποτέ τη νέα έκδοση
και το πρόβλημα παραμένει γι' αυτούς.

Παραγωγή → η νέα έκδοση → **«Ποσοστό κυκλοφορίας»** → 100% → Ενημέρωση.

## ΒΗΜΑ 8 — Περίμενε τον έλεγχο

Ο έλεγχος της Google παίρνει συνήθως **λίγες ώρες έως 2 μέρες**. Μέχρι να
ολοκληρωθεί, η σελίδα δείχνει ακόμα την παλιά έκδοση.

---

## Επαλήθευση ότι έπιασε

**Α. Στο Play Console** — Επισκόπηση εφαρμογής: η παραγωγή πρέπει να δείχνει
τον νέο `versionCode`, και κάθε κανάλι δοκιμής «Χωρίς ενεργή έκδοση».

**Β. Στη δημόσια σελίδα** — μετά τον έλεγχο, το `versionName` πρέπει να έχει
αλλάξει από 2.0.2:

```
https://play.google.com/store/apps/details?id=com.fooddaily.app
```

**Γ. Σε καθαρό κινητό** — απεγκατάσταση, εκκαθάριση δεδομένων του Play Store
(Ρυθμίσεις → Εφαρμογές → Google Play Store → Αποθηκευτικός χώρος →
Εκκαθάριση δεδομένων), επανεγκατάσταση, άνοιγμα της εφαρμογής.
Στη σελίδα του Play πρέπει πλέον να εμφανίζονται **αστέρια**, όχι «ιδιωτικό
σχόλιο».

Το «ιδιωτικό σχόλιο» είναι το σίγουρο σημάδι ότι η συσκευή θεωρείται ακόμα
δοκιμαστής. Αν επιμένει μετά από όλα τα παραπάνω, το πρόβλημα είναι στον
συγκεκριμένο λογαριασμό, όχι στην εφαρμογή — δοκίμασε με άλλον.

---

## Τι να ΜΗΝ κάνεις

- **Μην** αλλάξεις ποτέ το `applicationId` (`com.fooddaily.app`). Θεωρείται
  νέα εφαρμογή και χάνονται όλα: χρήστες, αξιολογήσεις, συνδρομές.
- **Μην** ανεβάσεις το νέο build σε κανάλι δοκιμής «για σιγουριά». Αυτό
  ακριβώς δημιούργησε το πρόβλημα.
- **Μην** αντικαταστήσεις το keystore. Χωρίς αυτό δεν ενημερώνεται ποτέ ξανά
  η εφαρμογή.
- **Μην** ανεβάσεις το keystore ή το JSON του service account σε git. Το
  repository είναι δημόσιο.

---

## Χρονοδιάγραμμα

| Πότε | Τι |
|---|---|
| Σήμερα | Βήματα 1–7 (~30 λεπτά) |
| +1 ώρα ως 2 μέρες | Έλεγχος Google, η έκδοση γίνεται ζωντανή |
| +1 μέρα μετά | Επαλήθευση Γ σε καθαρό κινητό |
| +3 έως 7 μέρες | Οι πρώτες αξιολογήσεις στα Στατιστικά → Βαθμολογίες |
