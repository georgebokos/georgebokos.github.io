# Νέο build στην παραγωγή — αναλυτικά βήματα

Στόχος: η **παραγωγή** να έχει τον **μεγαλύτερο `versionCode`** από κάθε
κανάλι δοκιμής, ώστε οι συσκευές να ξεκολλήσουν από το κανάλι δοκιμής και να
εμφανιστούν επιτέλους τα αστέρια αντί για «ιδιωτικό σχόλιο».

---

## ΤΟ ΑΡΧΕΙΟ ΠΟΥ ΑΝΕΒΑΖΕΙΣ

```
app-release.aab
```

Πλήρης διαδρομή, μετά το build:

```
fooddaily-app/android/app/build/outputs/bundle/release/app-release.aab
```

**Είναι `.aab`, όχι `.apk`.** Το Play Console δέχεται μόνο `.aab` για νέες
εκδόσεις από το 2021. Αν σου βγάλει `.apk`, έτρεξες λάθος εντολή (βλ. ΒΗΜΑ 5).

Μέγεθος: συνήθως 5–15 MB. Αν είναι μερικά KB, το build απέτυχε.

---

## ΤΑ ΠΡΟΓΡΑΜΜΑΤΑ ΠΟΥ ΘΑ ΧΡΕΙΑΣΤΕΙΣ

Μόνο **τρία**. Δεν χρειάζεται Android Studio.

| # | Πρόγραμμα | Τι είναι | Σε ποια βήματα |
|---|---|---|---|
| 1 | **Φυλλομετρητής** (Chrome/Edge/Safari) → `play.google.com/console` | Το Play Console είναι ιστοσελίδα, δεν εγκαθίσταται | ΒΗΜΑ 1, 2, 6, 7, 8 |
| 2 | **Τερματικό** (Terminal σε Mac/Linux, PowerShell σε Windows) | Εκεί γράφεις τις εντολές | ΒΗΜΑ 3, 5 |
| 3 | **Επεξεργαστής κειμένου** (VS Code, Notepad++, TextEdit — ή ό,τι έχεις) | Για να αλλάξεις 2 γραμμές σε ένα αρχείο | ΒΗΜΑ 4 |

Το τερματικό το ανοίγεις:
- **Mac**: Spotlight (⌘ + κενό) → γράψε `Terminal` → Enter
- **Windows**: πλήκτρο Windows → γράψε `PowerShell` → Enter
- **Linux**: Ctrl + Alt + T

Ο επεξεργαστής κειμένου πρέπει να αποθηκεύει **απλό κείμενο**. Το Word **δεν
κάνει** — χαλάει το αρχείο. Αν δεν έχεις τίποτα άλλο, στα Windows χρησιμοποίησε
το Σημειωματάριο (Notepad), στο Mac το TextEdit σε λειτουργία απλού κειμένου.

**Android Studio δεν χρειάζεται.** Χρειάζεται όμως να υπάρχει **Java (JDK 17+)**
στο μηχάνημα, αλλιώς το `./gradlew` δεν τρέχει. Έλεγχος στο τερματικό:

```bash
java -version
```

Αν τυπώσει έκδοση (π.χ. `openjdk version "17.0.x"`), είσαι εντάξει. Αν πει
«command not found», εγκατέστησε το **Temurin JDK 17** από το
`adoptium.net`. (Αν έχεις ξαναχτίσει την εφαρμογή σε αυτό το μηχάνημα, το
έχεις ήδη.)

Χρειάζεται επίσης **Node.js** για το `npm`:

```bash
node -v
```

---

## Πριν ξεκινήσεις: τι ΔΕΝ χρειάζεται build

Οι αλλαγές στο `index.html` και στο `service-worker.js` (κατώφλι προτροπής,
γερμανικά, διορθώσεις γιορτών) **είναι ήδη live** — η εφαρμογή φορτώνει το
site, δεν το πακετάρει. Το build χρειάζεται **μόνο** για:

1. το plugin του In-App Review (native κώδικας)
2. τον νέο `versionCode`

---

## ΒΗΜΑ 1 — Κατέγραψε τους τρέχοντες αριθμούς

> 🌐 **Πρόγραμμα: φυλλομετρητής** — `play.google.com/console`

Play Console → **Δοκιμή και κυκλοφορία** → **Επισκόπηση εφαρμογής**.
Σημείωσε τον `versionCode` κάθε καναλιού:

| Κανάλι | versionCode | Ενεργό; |
|---|---|---|
| Παραγωγή | ___ | |
| Ανοιχτή δοκιμή | ___ | |
| Κλειστή δοκιμή | ___ | |
| Εσωτερική δοκιμή | ___ | |

**Ο νέος αριθμός πρέπει να είναι μεγαλύτερος από ΟΛΟΥΣ**, όχι μόνο από της
παραγωγής. Αν κλειστή δοκιμή = 20 και παραγωγή = 20, βάλε **21**.

Το τρέχον το βλέπεις και τοπικά:

```bash
cd fooddaily-app
grep -n "versionCode\|versionName" android/app/build.gradle
```

## ΒΗΜΑ 2 — Σταμάτησε ΟΛΑ τα κανάλια δοκιμής

> 🌐 **Πρόγραμμα: φυλλομετρητής** — Play Console

Για καθένα από **εσωτερική, κλειστή, ανοιχτή** που έχει ενεργή κυκλοφορία:

Play Console → **Δοκιμή** → *(κανάλι)* → καρτέλα **Εκδόσεις** → στην ενεργή
έκδοση, μενού **⋮** → **«Διακοπή κυκλοφορίας»**.

Κάν' το **πριν** το ανέβασμα. Δεν χάνεις τίποτα: το build είναι ήδη στην
παραγωγή, οπότε όσοι το έχουν συνεχίζουν κανονικά.

## ΒΗΜΑ 3 — Εγκατάστησε το plugin

> ⌨️ **Πρόγραμμα: τερματικό**

### 3.1 Άνοιξε τερματικό στον σωστό φάκελο

```bash
cd fooddaily-app
```

Επιβεβαίωσε ότι είσαι στο σωστό σημείο — πρέπει να υπάρχουν και τα τρία:

```bash
ls package.json capacitor.config.* android
```

Αν σου πει «No such file or directory», είσαι σε λάθος φάκελο. Ο σωστός είναι
αυτός που περιέχει το `package.json` του Capacitor project, **όχι** ο φάκελος
του site.

### 3.2 Εγκατάσταση

```bash
npm install @capacitor-community/in-app-review
```

Τι πρέπει να δεις στο τέλος:

```
added 1 package, and audited NNN packages in Xs
```

Προειδοποιήσεις `npm warn deprecated` ή `N vulnerabilities` **αγνοούνται** —
δεν είναι σφάλματα. Σφάλμα είναι μόνο το `npm ERR!`.

Έλεγχος ότι μπήκε:

```bash
grep in-app-review package.json
```

Πρέπει να τυπώσει μια γραμμή σαν
`"@capacitor-community/in-app-review": "^X.Y.Z",`. Αν δεν τυπώσει τίποτα,
η εγκατάσταση απέτυχε — μην προχωρήσεις.

### 3.3 Συγχρονισμός με το Android project

```bash
npx cap sync android
```

Αυτό κάνει δύο πράγματα: αντιγράφει τα web assets και **καταχωρεί το plugin**
στο native project. Χωρίς αυτό, το plugin είναι εγκατεστημένο στο npm αλλά
δεν υπάρχει μέσα στην εφαρμογή.

Τι πρέπει να δεις:

```
✔ Copying web assets ...
✔ Updating Android plugins ...
[info] Found 6 Capacitor plugins for android:
       @capacitor-community/in-app-review@X.Y.Z
       @capacitor/app@...
       ...
✔ update android ...
✔ Sync finished
```

**Το κρίσιμο είναι να δεις το `in-app-review` μέσα σε αυτή τη λίστα.** Αν δεν
είναι εκεί, το build θα βγει μια χαρά αλλά η αξιολόγηση μέσα από την εφαρμογή
δεν θα δουλέψει (θα πέφτει στο Play Store, όπως σήμερα).

Δεν αγγίζεις **καθόλου** Java ή Kotlin — το Capacitor το κάνει μόνο του.

Αν το `npx cap sync` παραπονεθεί ότι δεν βρίσκει το `webDir`: το project
φορτώνει το live site, οπότε ο φάκελος αυτός μπορεί να είναι σχεδόν άδειος.
Φτιάχνεται με `mkdir -p <webDir>` (το όνομά του το λέει το
`capacitor.config.*`) και ξανατρέχεις το sync.

## ΒΗΜΑ 4 — Ανέβασε τον versionCode

> 📝 **Πρόγραμμα: επεξεργαστής κειμένου** (οι έλεγχοι 4.1/4.3 στο τερματικό)

### 4.1 Δες τι έχεις τώρα

```bash
grep -n "versionCode\|versionName" android/app/build.gradle
```

Παράδειγμα εξόδου:

```
14:        versionCode 20
15:        versionName "2.0.2"
```

### 4.2 Άνοιξε το αρχείο και άλλαξε δύο γραμμές

Αρχείο: `fooddaily-app/android/app/build.gradle`
(**όχι** το `android/build.gradle` — αυτό είναι άλλο αρχείο, ένα επίπεδο πάνω,
και δεν έχει `versionCode`.)

Βρες το μπλοκ `defaultConfig`:

```gradle
android {
    namespace "com.fooddaily.app"
    compileSdk rootProject.ext.compileSdkVersion
    defaultConfig {
        applicationId "com.fooddaily.app"
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode 20            // ← ΑΛΛΑΞΕ ΑΥΤΟ
        versionName "2.0.2"       // ← ΚΑΙ ΑΥΤΟ
        ...
    }
```

Γίνεται:

```gradle
        versionCode 21
        versionName "2.0.3"
```

**Ποιον αριθμό βάζεις:** τον **μεγαλύτερο** από τον πίνακα του ΒΗΜΑΤΟΣ 1, +1.
Αν παραγωγή = 20 και κλειστή δοκιμή = 20 → βάζεις **21**.
Αν παραγωγή = 20 αλλά κάποιο κανάλι δοκιμής έχει 22 → βάζεις **23**.

| | Τι είναι | Κανόνας |
|---|---|---|
| `versionCode` | ακέραιος, χωρίς εισαγωγικά | πάντα **μεγαλύτερος** από κάθε προηγούμενο· δεν τον βλέπει ο χρήστης |
| `versionName` | κείμενο, **με** εισαγωγικά | το βλέπει ο χρήστης στη σελίδα του Play· δεν επηρεάζει τίποτα τεχνικά |

⚠️ `applicationId "com.fooddaily.app"` — **δεν το αγγίζεις ποτέ.**

### 4.3 Επιβεβαίωσε την αλλαγή

```bash
grep -n "versionCode\|versionName" android/app/build.gradle
```

Πρέπει τώρα να δείχνει τους νέους αριθμούς. Αν δείχνει ακόμα τους παλιούς,
δεν αποθηκεύτηκε το αρχείο.

## ΒΗΜΑ 5 — Χτίσε το .aab

> ⌨️ **Πρόγραμμα: τερματικό**

### 5.1 Πήγαινε στον φάκελο android

```bash
cd android
```

Πλήρης διαδρομή: `fooddaily-app/android`. Εκεί υπάρχει το `gradlew`:

```bash
ls gradlew
```

Το `./gradlew` τρέχει **μέσα από αυτόν** τον φάκελο. Από αλλού δεν δουλεύει.

### 5.2 Καθάρισμα

```bash
./gradlew clean
```

Σε Windows: `gradlew.bat clean`.

Αυτό σβήνει τα προηγούμενα build. **Μην το παραλείψεις** — χωρίς αυτό το
Gradle μπορεί να επαναχρησιμοποιήσει παλιό αποτέλεσμα και να ανεβάσεις
κατά λάθος το build με τον **παλιό** `versionCode`.

Διαρκεί λίγα δευτερόλεπτα. Τέλος: `BUILD SUCCESSFUL`.

### 5.3 Το build

```bash
./gradlew bundleRelease
```

Σε Windows: `gradlew.bat bundleRelease`.

**Η σωστή εντολή είναι `bundleRelease`.** Προσοχή:

| Εντολή | Τι βγάζει | Για το Play Console |
|---|---|---|
| `bundleRelease` | `.aab` | ✅ αυτό θέλεις |
| `assembleRelease` | `.apk` | ❌ δεν γίνεται δεκτό |
| `assembleDebug` | `.apk` debug | ❌ ποτέ |

Την **πρώτη** φορά μπορεί να πάρει 3–10 λεπτά (κατεβάζει εξαρτήσεις). Μετά
1–2 λεπτά. Θα δεις πολλές γραμμές `> Task :app:...` — είναι φυσιολογικό.

Το ζητούμενο στο τέλος:

```
BUILD SUCCESSFUL in 2m 14s
```

Αν δεις `BUILD FAILED`, **σταμάτα** — μη συνεχίσεις στο βήμα 6. Δες την
ενότητα «Αν κάτι πάει στραβά» παρακάτω.

### 5.4 Βρες και έλεγξε το αρχείο

```bash
ls -lh app/build/outputs/bundle/release/
```

Πρέπει να δεις:

```
-rw-r--r--  1 user  staff   8.4M Sep 10 14:32 app-release.aab
```

Τρεις έλεγχοι πριν το ανεβάσεις:

1. **Η ημερομηνία είναι σημερινή** — αλλιώς κοιτάς παλιό αρχείο
2. **Το μέγεθος είναι MB, όχι KB** — μερικά KB σημαίνει αποτυχημένο build
3. **Ο `versionCode` μέσα του είναι ο νέος:**

```bash
unzip -p app-release.aab BUNDLE-METADATA/*/*.properties 2>/dev/null | head
```

Πιο αξιόπιστα, αν έχεις το `bundletool` ή το Android SDK:

```bash
aapt2 dump badging app-release.aab 2>/dev/null | grep versionCode
```

Αν καμία εντολή δεν δουλεύει, μην ανησυχείς: ο ίδιος έλεγχος γίνεται
οπωσδήποτε στο **ΒΗΜΑ 6, σημείο 2**, όπου το Play Console δείχνει τον αριθμό μετά
το ανέβασμα.

### Υπογραφή

Το `.aab` πρέπει να υπογραφεί με το **ίδιο `signing.keystore`** των
προηγούμενων εκδόσεων. Συνήθως είναι ήδη ρυθμισμένο σε `signingConfigs`
μέσα στο `build.gradle`, ή σε `keystore.properties`.

Αν το Play απορρίψει το ανέβασμα λέγοντας ότι το πιστοποιητικό δεν ταιριάζει,
**μην αλλάξεις keystore** — βρες το σωστό. Χωρίς αυτό η εφαρμογή δεν
ενημερώνεται ποτέ ξανά.

## ΒΗΜΑ 6 — Ανέβασέ το στην ΠΑΡΑΓΩΓΗ

> 🌐 **Πρόγραμμα: φυλλομετρητής** — Play Console

Play Console → **Δοκιμή και κυκλοφορία** → **Παραγωγή** →
κουμπί **«Δημιουργία νέας έκδοσης»** (πάνω δεξιά)

1. Στο πλαίσιο **«Πακέτα εφαρμογών»**, σύρε ή επίλεξε το
   `app-release.aab`
2. Περίμενε να ανέβει· θα δεις τον νέο `versionCode` δίπλα στο όνομα
   **Επιβεβαίωσε εδώ ότι είναι ο σωστός αριθμός** — αν δείχνει τον παλιό,
   ξέχασες το ΒΗΜΑ 4 ή δεν έτρεξες `clean`
3. **Όνομα έκδοσης**: συμπληρώνεται αυτόματα (π.χ. `21 (2.0.3)`)
4. **Σημειώσεις έκδοσης** — γράψε μέσα στις ετικέτες γλώσσας:

```
• Γερμανικά — όλες οι συνταγές και η εφαρμογή
• Αξιολόγηση μέσα από την εφαρμογή
• Διορθώσεις στις εορταστικές προτάσεις
```

5. **«Επόμενο»** → **«Αποθήκευση»** → **«Έλεγχος έκδοσης»**
6. **«Έναρξη κυκλοφορίας στην παραγωγή»** → επιβεβαίωση

## ΒΗΜΑ 7 — Κυκλοφορία στο 100%

> 🌐 **Πρόγραμμα: φυλλομετρητής** — Play Console

Αν στο βήμα 6 εμφανιστεί **«Σταδιακή κυκλοφορία»** με ποσοστό (π.χ. 20%),
βάλ' το **100%**. Με σταδιακή, ένα μέρος των χρηστών δεν παίρνει ποτέ τη νέα
έκδοση και το πρόβλημα παραμένει γι' αυτούς.

Παραγωγή → η νέα έκδοση → **«Ποσοστό κυκλοφορίας»** → 100% → Ενημέρωση.

## ΒΗΜΑ 8 — Περίμενε τον έλεγχο

> ☕ **Πρόγραμμα: κανένα** — περιμένεις email από την Google

Ο έλεγχος της Google παίρνει **λίγες ώρες έως 2 μέρες**. Μέχρι τότε η δημόσια
σελίδα δείχνει ακόμα την παλιά έκδοση. Θα λάβεις email όταν εγκριθεί.

---

## Επαλήθευση ότι έπιασε

**Α. Play Console → Επισκόπηση εφαρμογής**
Η παραγωγή δείχνει τον νέο `versionCode`, και κάθε κανάλι δοκιμής
«Χωρίς ενεργή έκδοση».

**Β. Δημόσια σελίδα** — μετά τον έλεγχο, το `versionName` έχει αλλάξει από
2.0.2:

```
https://play.google.com/store/apps/details?id=com.fooddaily.app
```

**Γ. Καθαρό κινητό** — απεγκατάσταση, εκκαθάριση δεδομένων του Play Store
(Ρυθμίσεις → Εφαρμογές → Google Play Store → Αποθηκευτικός χώρος →
Εκκαθάριση δεδομένων), επανεγκατάσταση, άνοιγμα της εφαρμογής.
Στη σελίδα του Play πρέπει να εμφανίζονται **αστέρια**, όχι «ιδιωτικό σχόλιο».

---

## Αν κάτι πάει στραβά

**«Δεν υποστηρίζεται ο τύπος αρχείου»**
Ανέβασες `.apk`. Τρέξε `bundleRelease`, όχι `assembleRelease`.

**«Ο κωδικός έκδοσης 20 έχει ήδη χρησιμοποιηθεί»**
Δεν άλλαξες τον `versionCode`, ή δεν έτρεξες `./gradlew clean` πριν.

**«Το πακέτο δεν είναι υπογεγραμμένο σωστά»**
Λάθος keystore. Μην το αντικαταστήσεις — βρες το αρχικό.

**Το `bundleRelease` αποτυγχάνει**
Τρέξε `./gradlew bundleRelease --stacktrace` και διάβασε την πρώτη γραμμή
`* What went wrong:`. Συνήθως λείπει το keystore ή έχει λάθος κωδικό.

**Δεν βρίσκω το `app-release.aab`**
```bash
cd fooddaily-app
find . -name "*.aab" -newermt "-1 hour"
```

---

## Τι να ΜΗΝ κάνεις

- **Μην** αλλάξεις ποτέ το `applicationId` (`com.fooddaily.app`). Θεωρείται
  νέα εφαρμογή και χάνονται όλα: χρήστες, αξιολογήσεις, συνδρομές.
- **Μην** ανεβάσεις το νέο build σε κανάλι δοκιμής «για σιγουριά». Αυτό
  ακριβώς δημιούργησε το πρόβλημα.
- **Μην** αντικαταστήσεις το keystore.
- **Μην** ανεβάσεις το keystore ή το JSON του service account σε git.
  Το repository είναι δημόσιο.

---

## Χρονοδιάγραμμα

| Πότε | Τι |
|---|---|
| Σήμερα | Βήματα 1–7 (~30 λεπτά) |
| +1 ώρα ως 2 μέρες | Έλεγχος Google, η έκδοση γίνεται ζωντανή |
| +1 μέρα μετά | Επαλήθευση Γ σε καθαρό κινητό |
| +3 έως 7 μέρες | Οι πρώτες αξιολογήσεις στα Στατιστικά → Βαθμολογίες |
