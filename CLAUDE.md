# FoodDaily — Οδηγίες project

Ελληνική εφαρμογή συνταγών (PWA + Android app στο Google Play).

## Γλώσσα επικοινωνίας
Απάντα **πάντα στα ελληνικά**.

## Δομή κώδικα
- `index.html` — **όλη** η εφαρμογή σε ένα αρχείο (HTML + CSS + JS, ~1.4 MB)
- `service-worker.js` — cache, ειδοποιήσεις, timer μαγειρικής
- `manifest.json` — PWA manifest
- `.well-known/assetlinks.json` — σύνδεση site ↔ Android app
- `images/` — φωτογραφίες συνταγών
- `build.py` + `.github/workflows/build-deploy.yml` — obfuscation και deploy στο `gh-pages`

### ⚠️ Οι φάκελοι `diy/`, `dances/` και `espa/` ΔΕΝ είναι FoodDaily

Στο ίδιο repository φιλοξενούνται και **εντελώς ξεχωριστές εφαρμογές**:

| Φάκελος | Εφαρμογή | Διεύθυνση |
|---|---|---|
| `diy/` | «Φτιάξ' το μόνος σου» — οδηγοί DIY | `/diy/` |
| `dances/` | «Ελληνικοί Παραδοσιακοί Χοροί» | `/dances/` |
| `espa/` | «ΕΣΠΑ Βοηθός» — ανάλυση πρόσκλησης & αίτηση στήριξης | `/espa/` |

Μοιράζονται με το FoodDaily **μόνο** το domain και τη διαδικασία deploy.
Καθεμιά έχει δικό της `manifest.json`, δικό της `sw.js` με δική του εμβέλεια,
δικά της εικονίδια και δικό της `CLAUDE.md`.

- Οι κανόνες αυτού του αρχείου (συνταγές, εικόνες, Play Store, συνδρομές) **δεν
  ισχύουν** εκεί, και οι κανόνες των `diy/CLAUDE.md`, `dances/CLAUDE.md` και
  `espa/CLAUDE.md` δεν ισχύουν εδώ.
- Αλλαγή σε αυτούς τους φακέλους **δεν** απαιτεί αύξηση του `VERSION` του
  `service-worker.js` του FoodDaily — καθεμιά έχει δικό της.
- Το `service-worker.js` του FoodDaily αγνοεί ρητά κάθε διαδρομή που ξεκινά με
  τα προθέματα του πίνακα `OTHER_APPS`. **Μην αφαιρέσεις αυτόν τον έλεγχο** και
  **πρόσθεσε** εκεί κάθε νέα εφαρμογή: χωρίς αυτόν το FoodDaily σερβίρει το δικό
  του `index.html` στις άλλες εφαρμογές όταν δεν υπάρχει σύνδεση.
- Το `build.py` τις αντιγράφει αυτούσιες (`COPY_DIRS`), χωρίς obfuscation.

**Κανόνας:** το FoodDaily δεν φιλοξενεί άλλες εφαρμογές μέσα του. Κάθε νέα
εφαρμογή πάει σε **δικό της φάκελο**, με δική της ταυτότητα, και δεν
δημιουργείται κανένας σύνδεσμος ή κοινός κώδικας ανάμεσά τους.

## Κανόνες εργασίας

**Push:** κάθε ολοκληρωμένη αλλαγή πάει σε **δύο** σημεία, χωρίς να το ζητήσει ο χρήστης:
```
git push -u origin <branch εργασίας>
git push origin <branch εργασίας>:main
```

**Service worker:** σε **κάθε** αλλαγή περιεχομένου, αύξησε τα `VERSION` και `CACHE` στο `service-worker.js` — αλλιώς οι χρήστες δεν παίρνουν την ενημέρωση.

**Διγλωσσία:** η εφαρμογή είναι ελληνικά/αγγλικά. Κάθε νέο κείμενο προς τον χρήστη πρέπει να μπαίνει **και στις δύο γλώσσες** (μέσω `curLang==='en'?...:...`, του πίνακα `TR`, ή των `data-i18n` attributes).

**Εικόνες:** κάθε νέα συνταγή χρειάζεται πραγματική φωτογραφία **χωρίς πνευματικά δικαιώματα** — αποκλειστικά από **Wikimedia Commons**. Καμία εικόνα με αμφίβολη άδεια.

Κάθε εικόνα που μπαίνει στο `images/` **συμπιέζεται πάντα πριν το commit**:
```python
from PIL import Image
im = Image.open(src).convert('RGB')
if im.width > 1000:
    im = im.resize((1000, round(im.height*1000/im.width)), Image.LANCZOS)
im.save(dst, 'JPEG', quality=82, optimize=True, progressive=True)
```
Μόνο `.jpg` — όχι PNG για φωτογραφίες. Στόχος: **80–150 KB** ανά εικόνα.
Μετά την προσθήκη, επαλήθευσε ότι κάθε αναφορά στο `MEAL_IMAGES`/`SWEET_IMAGES` αντιστοιχεί σε υπαρκτό αρχείο.

## Δεδομένα
`MEALS` (συνταγές), `SWEETS` (γλυκά), `FESTIVE` (γιορτές), `MEAL_PREP`, `DISLIKE_OPTS`.
Κάθε αντικείμενο έχει και πεδία με κατάληξη `_en` για τα αγγλικά (`ing_en`, `steps_en`, `n_en`, `d_en`, ...).

Το πλήθος συνταγών εμφανίζεται **δυναμικά** (hero υπότιτλος και `{N}` στο `ab_intro`) — δεν χρειάζεται χειροκίνητη ενημέρωση.

## Εγκεκριμένη λίστα νέων συνταγών (προς προσθήκη όταν το ζητήσει ο χρήστης)

Ελέγχθηκε ότι **καμία δεν υπάρχει** στις υπάρχουσες συνταγές.

**Καθημερινά:** Πουρές πατάτας · Μακαρόνια ναπολιτέν · Σνίτσελ χοιρινό · Πατάτες γιαχνί · Σαλάτα τόνου · Σαλάτα ζυμαρικών · Σούπα με φιδέ · Στιρ φράι κοτόπουλο · Φαλάφελ · Κεμπάπ σπιτικό · Αυγά γεμιστά

**Επίσημα:** Μοσχάρι Ουέλινγκτον · Κόκορας κρασάτος · Μπεφ μπουργκινιόν · Οσομπούκο με ριζότο μιλανέζε · Μοσχάρι στη γάστρα · Καρέ αρνιού με κρούστα βοτάνων · Χοιρινή πανσέτα σιγοψημένη · Σολομός σε φύλλο κρούστας

**Κατσικάκι** (δεν υπάρχει καμία συνταγή με κατσίκι — επιβεβαιώθηκε με έλεγχο): Κατσικάκι στον φούρνο με πατάτες · Κατσικάκι λεμονάτο με ρίγανη · Κατσικάκι γιουβέτσι με κριθαράκι · Κατσικάκι λαδορίγανη στη λαδόκολλα · Κατσικάκι φρικασέ με μαρούλι & άνηθο

**Παραδοσιακά:** Σπετζοφάι Πηλίου · Πατσάς · Καλιτσούνια Κρήτης · Μπουρέκι Χανίων · Σαλιγκάρια μπουμπουριστά · Τσιγαρέλι · Μπακαλιάρος με πράσα

**Γλυκά — γιορτινά:** Δίπλες · Τρουφάκια σοκολάτας · Κέικ γιαουρτιού

**Γλυκά — χωρίς φούρνο / ψυγείου:** Γλυκό ψυγείου με μπισκότα · Μους λεμονιού · Ζελέ με φρούτα · Παγωτό σπιτικό (2 υλικά) · Banoffee · Τούρτα παγωτό · Σεμιφρέντο

**Γλυκά — φούρνου, καθημερινά:** Πάστα φλώρα · Μάφιν σοκολάτας · Cupcakes με βουτυρόκρεμα · Μηλόπιτα ρολό (στρούντελ) · Πουτίγκα ψωμιού · Κέικ ανάποδο με ανανά

**Γλυκά — παραδοσιακά:** Τουλούμπες · Καριόκα · Γλυκό κουταλιού βύσσινο · Γλυκό κουταλιού καρυδάκι · Χαλβάς με ταχίνι · Λουκούμια σπιτικά · Μελόπιτα Σίφνου

**Γλυκά — βάσεις:** Κρέμα ζαχαροπλαστικής · Σπιτική μαρμελάδα

### ⚠️ Τα γλυκά πάνε στο MEALS, όχι στο SWEETS

Η ενότητα `SWEETS` **δεν είναι προσβάσιμη**: το μοναδικό κουμπί που οδηγεί σε αυτήν
βρίσκεται σε γραμμή με `display:none` («Legacy ntab, kept for JS compat»). Η κάτω μπάρα
δεν έχει κουμπί Γλυκά. Ό,τι μπει στο `SWEETS` δεν το βλέπει κανείς.

**Κάθε νέο γλυκό μπαίνει στο `MEALS` με `cats:['desserts']`**, όπου εμφανίζεται στην
κατηγορία «🍮 Γλυκά» των ΚΑΤΗΓΟΡΙΩΝ ΦΑΓΗΤΟΥ.

**Απορρίφθηκαν από τον χρήστη** (μην τα προτείνεις ξανά): γύρος, αστακός, πάπια, χριστόψωμο, χαλβάς Φαρσάλων, καρπάτσιο, γρανίτα λεμονιού, γιαούρτι με μέλι & καρύδια.

Σύνολο εγκεκριμένων: **56** (31 φαγητά + 25 γλυκά). Η προσθήκη γίνεται **μόνο** όταν το ζητήσει ρητά ο χρήστης, με προσοχή και σταδιακά.

### Μενού καλεσμένων — μόνιμος κανόνας

Κάθε φορά που προστίθενται συνταγές, οι **πιο εντυπωσιακές** μπαίνουν και στο μενού «👥 Καλεσμένοι» (`selGuest()` στο `index.html`), στους πίνακες προτεραιότητας:

- `P4` — εντυπωσιακά πιάτα ειδικής περίστασης (σούβλα, γουρουνόπουλο, Ουέλινγκτον)
- `P3` — εκλεκτά κυρίως πιάτα (φούρνου, θαλασσινά, μοσχάρι)
- `P2` — καλά οικογενειακά πιάτα

Η πρόταση για το ποιες μπαίνουν και σε ποιο επίπεδο **αναφέρεται πάντα ξεχωριστά στον χρήστη**, ώστε να διαλέξει ο ίδιος. Καμία δεν μπαίνει χωρίς έγκριση.

Κάθε νέα συνταγή χρειάζεται: `n`/`en`, `e`, `cats`, `cal`, `time`, `srv`, `pop`, `cost`, `cps`, `ing`/`ing_en`, `steps`/`steps_en`, `wine`/`wine_en`, `plate`/`plate_en`, θρεπτικά, εγγραφή στο `MEAL_IMAGES` και συμπιεσμένη φωτογραφία.

## Διανομή στο Google Play

- **Package ID:** `com.fooddaily.app` (δεν αλλάζει ποτέ — αλλιώς θεωρείται νέα εφαρμογή)
- **versionCode:** πρέπει **πάντα** να αυξάνεται
- **Σε παραγωγή βρίσκεται η Capacitor έκδοση** — αυτή έχει τις λειτουργικές πληρωμές
- Η TWA είναι η εγκεκριμένη παλιά έκδοση· δεν ξαναχτίζεται

### Πώς φτάνουν οι αλλαγές στους χρήστες

| Είδος αλλαγής | Πού εμφανίζεται |
|---|---|
| Web κώδικας (`index.html`, `service-worker.js`) | **Αυτόματα και στις δύο εκδόσεις**, χωρίς build |
| Native (plugin, δικαιώματα, εικονίδιο, versionCode) | Μόνο σε όποια έκδοση χτιστεί και ανέβει — δηλαδή στην Capacitor |

Το package ID είναι κοινό, οπότε για το Play πρόκειται για **μία εφαρμογή**. Το Play σερβίρει
πάντα το μεγαλύτερο διαθέσιμο `versionCode`: όσοι έχουν TWA παίρνουν **κανονική αυτόματη ενημέρωση**
σε Capacitor, χωρίς απεγκατάσταση και χωρίς απώλεια δεδομένων ή συνδρομών.

⚠️ Εξαίρεση: αν σε κανάλι δοκιμής υπάρχει build με `versionCode` **μεγαλύτερο** από της παραγωγής,
οι δοκιμαστές εκείνου του καναλιού μένουν εκεί. Χρειάζεται έλεγχος και διακοπή της κυκλοφορίας.

Καθώς οι χρήστες περνούν σε Capacitor, ο έλεγχος `hasNativeBilling()` γίνεται αληθής και το paywall
ενεργοποιείται **μόνο του** — δεν απαιτείται καμία ενέργεια. Όσοι μένουν σε παλιά TWA έχουν
Premium δωρεάν, όπως ορίζει ο κανόνας συμβατότητας.

### Δύο εκδόσεις της Android εφαρμογής
1. **TWA** (τρέχουσα, υποβλήθηκε για έλεγχο παραγωγής) — τοπικό project, χτίζεται με Bubblewrap
2. **Capacitor** (υπό δοκιμή) — τοπικό project `fooddaily-app`, ενσωματωμένο WebView, native πληρωμές

Και οι δύο φορτώνουν το **ίδιο live site**. Αλλαγές στον web κώδικα εμφανίζονται αμέσως και στις δύο, χωρίς νέο build.

### ✅ Η πρόσβαση παραγωγής εγκρίθηκε (6 Αυγ 2026)

Η απαίτηση των 12+ δοκιμαστών για 14 ημέρες ολοκληρώθηκε. Πλέον επιτρέπεται δημοσίευση στην παραγωγή, και **δεν απαιτείται ξανά κλειστή δοκιμή** για μελλοντικές ενημερώσεις.

Επόμενο βήμα: δημοσίευση της **Capacitor** έκδοσης στην παραγωγή (όχι της TWA), γιατί μόνο αυτή έχει λειτουργικές πληρωμές και συμμορφώνεται με την απαίτηση Play Billing Library 8 (προθεσμία 31/8/2026).

### Κανόνας συμβατότητας TWA

Όσο η TWA κυκλοφορεί ακόμα σε οποιοδήποτε κανάλι, οι λειτουργίες που δεν δουλεύουν εκεί παραμένουν απενεργοποιημένες.

**Κάθε λειτουργία που δεν μπορεί να δουλέψει σωστά στην TWA πρέπει να απενεργοποιείται εκεί**, με έλεγχο ύπαρξης του αντίστοιχου Capacitor plugin:

| Λειτουργία | Έλεγχος |
|---|---|
| Paywall / συνδρομές | `hasNativeBilling()` |
| Ειδοποιήσεις | `isNativeApp()` |
| Κουμπί «πίσω» | `Capacitor.Plugins.App` |
| Εκφώνηση | `ttsPlugin()` |
| Κοινοποίηση | `sharePlugin()` |

Στην TWA δεν εμφανίζεται προτροπή συνδρομής, γιατί οι αγορές εκεί εξαρτώνται από τον browser και συχνά αποτυγχάνουν. Η ακριβής συμπεριφορά ορίζεται στο `getPremiumState()` του `index.html`.

### ⚠️ TWA και σκέτος browser ΔΕΝ είναι το ίδιο

Καμία από τις δύο δεν έχει Capacitor, οπότε το `hasNativeBilling()` επιστρέφει
`false` και στις δύο. Παλαιότερα ο κανόνας «χωρίς native πληρωμές ⇒ όλα ξεκλείδωτα»
έπιανε **και τον browser** — δηλαδή όποιος άνοιγε το site έπαιρνε ολόκληρο το
Premium δωρεάν και για πάντα.

Ο διαχωρισμός γίνεται με το `isTwa()`, που ελέγχει `document.referrer` ίσο με
`android-app://com.fooddaily.app` και αποθηκεύει το αποτέλεσμα στο `fd_twa`
(ο referrer χάνεται στις επόμενες πλοηγήσεις).

| Πλατφόρμα | Κατάσταση | Γιατί |
|---|---|---|
| **Capacitor** | 3ήμερη γνωριμία → paywall | Έχει λειτουργικές πληρωμές |
| **TWA** | πάντα ξεκλείδωτη | Αδιέξοδο paywall — δεν ολοκληρώνεται αγορά |
| **Browser / PWA** | 3ήμερη γνωριμία → paywall | Το paywall οδηγεί στο Google Play |

Στον browser το κουμπί του paywall **δεν επιχειρεί αγορά** — ανοίγει το
`PLAY_URL`, γιατί η συνδρομή αποκτάται μόνο μέσα από την εφαρμογή.

Η σελίδα `install.html` **δεν προτείνει** άνοιγμα στον browser, για τον ίδιο λόγο.

Σειρά: **TWA → έγκριση παραγωγής → μετά ανεβαίνει η Capacitor** ως επίσημη έκδοση.

## Συνδρομές Premium

- `premium_monthly` — 1,99€/μήνα (base plan `monthly`)
- `premium_yearly` — 14,99€/χρόνο (base plan `yearly`)
- **RevenueCat** entitlement: `FoodDaily Pro`
- Public SDK key (Android): `goog_wlLGtgfQNNakMXhZIqFioFaiPoX`

Στο `purchasePremium()` η σειρά προτεραιότητας είναι:
1. RevenueCat (Capacitor, native) — ανεξάρτητο από browser
2. Digital Goods API (TWA μέσω Chrome)
3. Ενημερωτικό μήνυμα

**Μην αφαιρέσεις τη διαδρομή 2** — χρησιμοποιείται από την έκδοση που έχουν σήμερα οι δοκιμαστές.

## Γνωστά θέματα

- Το **Brave** δεν υποστηρίζει Google Play Billing → η TWA έκδοση δεν μπορεί να κάνει αγορές εκεί. Λύνεται με την Capacitor έκδοση.
- Η Google απαιτεί **Play Billing Library 8** μέχρι 31/8/2026. Η TWA δεν μπορεί να συμμορφωθεί (η βιβλιοθήκη της έχει κολλήσει στην 7.1.1) — γι' αυτό η μετάβαση σε Capacitor είναι απαραίτητη.
- Το WebView **δεν** υποστηρίζει ειδοποιήσεις service worker. Στην Capacitor έκδοση πρέπει να ξαναγραφτούν με `@capacitor/local-notifications`.

## Ασφάλεια
Το `signing.keystore` και το JSON του service account είναι **μυστικά** — ποτέ σε commit, ποτέ σε δημόσιο αρχείο.

⚠️ **Το repository είναι δημόσιο.** Ό,τι γράφεται εδώ το διαβάζει ο καθένας στο
`github.com/georgebokos/georgebokos.github.io`. Μην καταγράφεις σε αυτό το αρχείο
τοπικές διαδρομές, κωδικούς, ή λεπτομέρειες για το πώς παρακάμπτεται μια πληρωμή.

Το `build.py` δημοσιεύει **ρητή λίστα** αρχείων ρίζας (`manifest.json`, `privacy.html`,
`dances.html`, εικονίδια). Το `CLAUDE.md` δεν ανεβαίνει πλέον στο site — παλαιότερα
ανέβαινε και ήταν προσβάσιμο στο `georgebokos.github.io/CLAUDE.md`.

Το public SDK key του RevenueCat είναι σχεδιασμένο να ενσωματώνεται σε client και
**δεν** είναι μυστικό.
